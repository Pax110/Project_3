import { GrUserSettings } from "react-icons/gr";
import styled from "styled-components";

export const UserSettings = styled(GrUserSettings)`
  color: rgba(0, 0, 255);
  width: 25px;
  height: 25px;
`;
